/**
 * 
 * O type any permite que você salve valores de qualquer tipo em uma variável (não é recomendado seu uso em qualquer hora, pois isso "anula" o uso do typescript)
 * 
 * 
 */

const names:any = ['michael', 'matheus', 'paulo', 'joão', 'pedro'];

names.push(556);